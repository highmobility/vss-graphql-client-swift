import Artemis

public enum BodyWindshieldRearWipingStatusEnum: String, Enum {
    case off = "off"
    case slow = "slow"
    case medium = "medium"
    case fast = "fast"
    case interval = "interval"
    case rainsensor = "rainsensor"
}
