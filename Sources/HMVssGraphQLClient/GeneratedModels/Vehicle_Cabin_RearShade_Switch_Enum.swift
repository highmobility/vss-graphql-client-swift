import Artemis

public enum CabinRearShadeSwitchEnum: String, Enum {
    case Inactive = "Inactive"
    case Close = "Close"
    case Open = "Open"
    case OneShotClose = "OneShotClose"
    case OneShotOpen = "OneShotOpen"
}
