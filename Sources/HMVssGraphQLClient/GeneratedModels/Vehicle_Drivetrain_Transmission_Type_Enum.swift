import Artemis

public enum DrivetrainTransmissionTypeEnum: String, Enum {
    case unknown = "unknown"
    case sequential = "sequential"
    case H = "H"
    case automatic = "automatic"
    case DSG = "DSG"
    case CVT = "CVT"
}
